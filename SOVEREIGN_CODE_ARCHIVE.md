# أرشيف الشفرة السيادية المحصنة (SOVEREIGN CODE ARCHIVE)
* **المستند:** SOVEREIGN_CODE_ARCHIVE.md
* **الإصدار المعماري المرجعي:** V2.6-Secured (المبني على دستور القوانين المختصر V5.5 والنبض الميداني الموحد)
* **تاريخ وميض التعديل الأول:** 2026-06-29
* **حالة النواة والتحصين:** V2.6-Secured - مدمج ومغلق بالكامل بموجب بروتوكولات الحماية المتقدمة وقفل النواة.

---

## البند الأول: تفاصيل العملية الفنية والترقيع الجراحي رقم [SR-2943]

### 1. الغايات والأهداف:
* سد التضارب المهني وتكامل الهوية البصرية والميدانية للكابتن والناقل السيادي (Sovereign Carrier).
* حظر التعتيم الجنائي على أخطاء المزامنة السحابية وتفعيل المعالج الجنائي الموحد `handleFirestoreError` تماشياً مع بروتوكول حماية الموارد رقم 88.
* توطيد المظهر الهيكلي والبيانات المفقودة في شاشة "حسابي" لتوحيد الرؤية ومؤشرات الامتثال الميداني للرادار (SSOT).

### 2. التغييرات الهندسية المدخلة:
* **الملف المجرى عليه التعديل:** `/src/components/dashboard/profile-tab.tsx`
* **المحاذاة والترقيعات المدخلة:**
  1. **الرتبة الميدانية الرسمية (Sovereign Rank Indicator):** حقن مؤشر بصري ديناميكي بجانب الاسم والرمز الجينومي يعرض رتب الكابتن المعتمدة (`Platinum`, `Gold`, `Silver`, `Bronze`) مع تدرجات لونية ميتاليكية وذهبية فاخرة مستخرجة من مصفوفة الألوان السيادية.
  2. **لوحة الجانب القانونية للمركبة (vehicle.sideId):** إدراج حقل إدخال إضافي مقيد ومحمي من الفقدان (مزامنة المسودات والتحديث السحابي التراكمي المدمج) مخصص لرقم لوحة الجانب القانوني الموثق مع تنسيق متوازن وعرض مصفوفة ثلاثية الأعمدة (اللوحة، رقم الجانب، سنة الصنع).
  3. **الارتباط والتبعية (Affiliation Info):** عرض بطاقة فرعية متوازنة توضح الجهة المظلية ونوع الارتباط للناقل بدقة وثبات.
  4. **مؤشرات المناعة السلوكية والعدادات التحذيرية:** عرض عدد الإلغاءات المتتالية النشطة السارية على الحافة وحالة امتثال رصيد الثقة الميداني لحظر السقوط في فخ التطهير السلوكي التلقائي (النزول تحت 4.2).
  5. **محاذاة معالج الأخطاء الجنائية (handleFirestoreError):** استبدال هيكلية الالتقاط المحلية بـ `handleFirestoreError` معززة بنوع العملية `OperationType.WRITE` والمستند المرجعي لضمان الحصانة السحابية للرادار.

---

## البند الثاني: ميثاق الامتثال الدستوري الأول (Constitutional Compliance Check)

* **CSS Freeze:** تم المحافظة على التجميد الكامل للتصميم ولم يتم تكسير أي وظائف أو مسارات مرئية، والاعتماد الكلي على عناصر الـ Bento-Grid لتفادي التكديس والتداخل العشوائي.
* **حماية الموارد:** صفر مستمعين عشوائيين لقاعدة البيانات، والالتزام المطلق بالكائن المرجعي الممرر لحظر استنزاف كاش المتصفح وقدرات هاتف المستخدم.
* **الجودة الماسية وسرعة المزامنة:** تم تشغيل مدقق لغة التايب سكريبت ومحاذاة المخرجات بنجاح تام وححصل التطبيق العملياتي على الجودة الماسية بصفر أخطاء تركيبية وصفر تحذيرات لغوية.

---

## البند الثالث: دستور التقنية والمعمارية النسيجية الحافة (الإصدار الشامل الموحد V2.6-Secured)

* **طبيعة الوثيقة:** معمارية هندسية قطعية ومغلقة لحماية الموارد وصفرية التكلفة الحافة.

### 1. معمارية النظام والمحددات الهندسية الكبرى (The Tech Stack)
* **بيئة العمل والواجهات:** تطبيق ويب تقدمي عالي الأداء ($PWA$) يعمل كواجهة مستقلة تماماً ($Standalone$) وبصيغة دون اتصال أولاً ($Offline-First$).
* **معمارية الحافة وصفرية التكلفة (SC55):** يُنقل كامل ثقل المعالجة الرياضية، والفرز، وعمليات مقارنة الأسعار، وحساب العدادات من السيرفر الأساسي لتتم داخل معالج هاتف المستخدم مباشرة ($Client-Side$).
* **قواعد البيانات والتخزين المحلي:** الاعتماد الكامل على الذاكرة المحلية المستقرة للمتصفح ($IndexedDB$ المدار عبر مكتبات سريعة مثل $Dexie.js$ أو $LocalStorage$).
* **المحكم الرقمي القطعي (SSOT) والخرائط الحرة:** تُستأصل واجهات جوجل المدفوعة استئصالاً جراحياً كاملاً. تُعتمد حسابات المسافة والوقت رياضياً على "حافة الشبكة" عبر مكتبات `MapLibre GL JS` و `h3-js` لتعمل على كرت الشاشة للحافة ($GPU\ Vector\ Tiles$).
* **خادم التوجيه الاستراتيجي المستقل (Self-Hosted OSRM):** يتم ربط النظام بخادم افتراضي ($VPS$) مستقل ومقطوع التكلفة ومحمي بذاكرة مساندة ($2GB\ Swap$) يستضيف محرك `OSRM` ومحمل عليه ملف خريطة الأردن المدمج ($25MB$). يتصل التطبيق بالخادم عبر استعلامات $HTTP\ REST\ API$ محلية فائقة السرعة، مغلفة بوكيل عكسي آمن ($Nginx$) وشهادة تشفير سيادية ($HTTPS$ عبر $Let's\ Encrypt$) لمنع ثغرة الـ `Mixed Content Block`.

### 2. هندسة التسعير النسيجية وبروتوكول عوامة المسح وعروض الناقلين
يتم احتساب السعر بدقة متناهية عبر دمج ثلاثية: **(إحداثيات الحافة + خلايا H3 النسيجية + مسافة شوارع OSRM الحقيقية)** لمنع ظلم السائق أو إفساد مؤشر التوازن.

#### أولاً: ميكانيكية العوامة الصامتة للراكب (Rider Drop & Wait)
1. **تأكيد السيادة وصفر تعقيد:** لا توجد أي حقول إدخال أسعار أو خيارات مفرطة في واجهة الراكب. الراكب يضغط زراً واحداً فقط: "اطلب الآن". الراكب لا يقدم أي أسعار، بل يرمي العوامة صامتاً وينتظر عروض السائقين.
2. **لحظة الارتكاز وتحويل الإحداثيات:** يقوم هاتف الراكب محلياً بتحويل إحداثيات الـ $GPS$ فوراً عبر مكتبة `h3-js` إلى الخلية السداسية المركزية الأساسية بدقة **ريزولوشن 9 (Resolution 9)** والتي تُمثّل "عوامة الارتكاز".
3. **النبض البصري التركوازي الحاكم وعزل الإعلانات:** بمجرد رمي العوامة، تنبثق شاشة الطلب كطبقة علوية كاملة ($Fullscreen\ Modal$) تحجب النهر الإعلاني تماماً لتركيز انتباه المستخدم، وتبث موجات دائرية مرسومة عبر كرت الشاشة ($GPU$) على شعاع $1.5\ \text{كم}$ حتمي لإعطاء الراكب إيحاءً هادئاً بالمسح الجغرافي.

#### ثانياً: آلية الاحتساب والتسعير عند الناقل (السائق)
1. يرى السائق طلب الراكب العائم في صالة المزاد إذا كان داخلاً في حلقة الزخم الجغرافي للمحيط.
2. يتم جلب قيم وثيقة النبض العام للمنطقة المخزنة محلياً لمرة واحدة ($1\ Read$) لتطبيق معادلة التسعير الأساسية:

$$\text{Fare} = \text{Base Fare} + (D_{\text{OSRM}} \times \text{Tariff Per Km}) + (T_{\text{OSRM}} \times \text{Tariff Per Min})$$

3. يقدم السائق عرضه المالي، ويمر السعر فوراً عبر فلاتر "كوابح السوق" (مؤشر التوازن 15%):
   * **منطقة الانحراف (10% إلى 14.9%):** وميض برتقالي حذر (`Amber Warning`). تُخفض رتبة السائق آلياً، وتزاح الحزمة للاحتياط، ويظهر للراكب وسم "سعر محروق قد يؤثر على الجودة".
   * **منطقة الحظر والمنع (15% فما فوق):** الإغلاق القرمزي (`Crimson Block & Input Lockout`). يُقفل حقل إدخال التسعير تماماً، ويتجمد زر "إرسال العرض" برمجياً لمنع النبض الزائف.

#### ثالثاً: بروتوكول الاستجابة الفورية عند انعدام الكباتن (The Zero-Captain Fallout)
1. يستمر النظام في جلب وعرض عروض السائقين المتنافسين على شاشة الراكب على شكل بطاقات منبثقة متتالية يختار منها الأنسب له بصفر نقرات زائدة.
2. إذا انتهت الـ **180 ثانية حتمية (3 دقائق)** دون أن يقدم أي سائق عرضاً في هذا المحيط، يتم إغلاق الرادار ميكانيكياً، وتذوب العوامة، وتنبثق رسالة حازمة تعلو المسرح الإعلاني:
   > 🛑 "لا يوجد سائقين متوفرين في محيطك الحالي، يرجى إعادة المحاولة بعد قليل."
3. فوراً، تختفي الطبقة العلوية تماماً وتذوب في العدم البرمجي بصيغة النعومة الانسيابية (`Transition CSS`)، ويعود الراكب تلقائياً لقرنية الشاشة الرئيسية مستمتعاً بالنهر الإعلاني دائم الدوران دون خيارات معقدة.

#### رابعاً: بروتوكول الهبوط الملاحي الآمن للتحكيم (Offline Fallback)
* في حال حدوث خطأ شبكي أو عدم استجابة خادم $OSRM$ في زمن يتجاوز $1500ms$، ينشط فوراً محرك الطوارئ الحافة المتكامل رياضياً بحساب مسافة "الهرسين" ($Haversine$) الجغرافية بين نقطتي الانطلاق والوصول مباشرة على معالج الهاتف، ويتم تعديل المسافة بضربها في معامل شوارع المملكة الثابت ($1.3$) لضمان استمرار عملية استقبال وعرض العروض بصفر انقطاع.

---

## البند الرابع: المحركات الهندسية الخمسة المستردة وجدار الحماية الجنائي

### 1. المحركات الهندسية المستردة:
1. **محرك الشحن الجغرافي اللامركزي:** الربط بقنوات الدفع الرقمية المحلية الأردنية ($CliQ$, زين كاش، أورنج ماني، إي فواتيركم) بناءً على حقل لواء الموطن ($homeDistrict$) وتأكيد المعاملة بنبضة واحدة ($1\ Write\ Only$).
2. **محرك الارتحال والذوبان النسيجي:** تحويل الإحداثيات محلياً إلى خلية $H3$ بدقة ريزولوشن 9. فور إنهاء الرحلة، يذوب حساب السائق في حلقة الزخم النسيجية لتلك المنطقة ($currentH3Cell$).
3. **معمارية حماية مساحة الـ PWA وقانون سقوط الأجل:** مسح سجلات الرحلات إحصائياً من السيرفر كل 72 ساعة حتمية، وإعدام المواد الإعلانية المحفوظة في المتصفح ميكانيكياً بعد 30 يوماً حتمية مقفلة برمز ثابت (`USER_VAULT_LIFETIME_MS`).
4. **محرك الروابط العميقة والاستحواذ المباشر:** حظر خروج المستخدم من التطبيق؛ وتفعيل أزرار الـ $Zero-Click$ الذرية عبر بروتوكولات التشغيل القياسية (`tel:` و `https://wa.me/`).
5. **بنية الصندوق الأسود الإحصائية للمندوبين:** جرد واستحقاق عمولة المندوبين الصافية مشروط بمرور 30 يوماً حتمية ومستقرة (`STABILITY_THRESHOLD_MS`) لنشاط الحساب المجتلب دون حظر أو حذف.

### 2. جدار الحماية الجنائي وقفل الثغرات الثلاث (The Kernel Anti-Cheat)
* **ثغرة التلاعب بالوقت المحلي للهاتف:** حظر الاعتماد على ساعة الهاتف، واعتماد "النبض الشبكي التفاضلي" (`Network Time Delta`) المقروء عند أول سحب لوثيقة النبض العام.
* **ثغرة التحديث الشبكي الأعمى عند انقطاع التغطية:** حبس حالة العداد محلياً في ملف مشفر ومحمي بختم رقمي مجزأ (`Cryptographic Hash`)، وحظر دخول صالة المزاد إلا بعد إتمام "المصافحة التصفوية الصامتة".
* **ثغرة إغراق خلايا H3 بطلبات وهمية:** حظر بث أكثر من طلب نشط واحد متزامن للراكب. وتفعيل كبح الإلغاء المتتالي؛ إذا ألغى الراكب 3 مرات متتالية، يُخصم $0.5$ من رصيد مناعته السلوكية، وإذا هبط عن $4.2$ يُسقط حسابه كلياً بروتوكول التطهير التلقائي.

### 3. قفل الحزمة البرمجية وتحصين النواة (Kernel Lockdown)

```typescript
// src/core/RadarAntiCheatKernel.ts
// إنفاذ التجميد الدستوري وتثبيت قفل مسح العوامة الصامتة وحظر التلاعب (V2.6-Secured)

export const RadarAntiCheatKernel = Object.freeze({
  
  validateDeviceTime: (networkTime: number, localTime: number): void => {
    const networkDelta = Math.abs(networkTime - localTime);
    if (networkDelta > 60000) { 
      throw new Error("SECURITY_ALERT: Device time manipulation detected. Kernel locked.");
    }
  },
  
  /**
   * صمام الأمان الجنائي لعوامة المسح (1.5 كم صارمة)
   * يرفض ويحجب أوتوماتيكياً أي عرض سعر قادم من سائق يتجاوز محيط العوامة المحدد للراكب
   */
  validateBuoyProximity: (riderLat: number, riderLon: number, driverLat: number, driverLon: number): boolean => {
    const R = 6371000; // نصف قطر الأرض بالمتر
    const dLat = (driverLat - riderLat) * Math.PI / 180;
    const dLon = (driverLon - riderLon) * Math.PI / 180;
    
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
              Math.cos(riderLat * Math.PI / 180) * Math.cos(driverLat * Math.PI / 180) *
              Math.sin(dLon / 2) * Math.sin(dLon / 2);
    
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
    const distanceMeters = R * c;

    // قفل الحظر الصارم: 1500 متر حتمية (1.5 كم)
    if (distanceMeters > 1500) {
      throw new Error("SECURITY_ALERT: Driver proposal blocked. Outside the 1.5km sovereign perimeter.");
    }
    
    return true; 
  },

  /**
   * محرك الاستعلام من الحافة وإدارة الطوارئ الجغرافية
   */
  fetchSovereignRoute: async (
    osrmServerUrl: string, 
    originLngLat: [number, number], 
    destLngLat: [number, number]
  ): Promise<{ distanceKm: number; durationMins: number; isFallback: boolean }> => {
    const endpoint = `${osrmServerUrl}/route/v1/driving/${originLngLat[0]},${originLngLat[1]};${destLngLat[0]},${destLngLat[1]}?overview=false`;
    try {
      const response = await fetch(endpoint, { method: 'GET', headers: { 'Content-Type': 'application/json' }, cache: 'no-cache' });
      if (!response.ok) throw new Error('OSRM_NETWORK_REJECTED');
      const data = await response.json();
      if (data.code !== 'Ok' || !data.routes || data.routes.length === 0) throw new Error('ROUTE_NOT_FOUND');

      return {
        distanceKm: parseFloat((data.routes[0].distance / 1000).toFixed(2)),
        durationMins: Math.ceil(data.routes[0].duration / 60),
        isFallback: false
      };
    } catch (error) {
      // السقوط الآمن محلياً كلياً بدون إنترنت عند تعذر السيرفر (معامل الالتواء للأردن 1.3)
      const fallbackDistanceMeters = RadarAntiCheatKernel.calculateFallbackDistance(originLngLat[1], originLngLat[0], destLngLat[1], destLngLat[0]);
      return {
        distanceKm: parseFloat((fallbackDistanceMeters / 1000).toFixed(2)),
        durationMins: Math.ceil((fallbackDistanceMeters / 1000) * 1.5),
        isFallback: true
      };
    }
  },

  calculateFallbackDistance: (lat1: number, lon1: number, lat2: number, lon2: number): number => {
    const R = 6371000; 
    const dLat = (lat2 - lat1) * Math.PI / 180;
    const dLon = (lon2 - lon1) * Math.PI / 180;
    const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) + Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) * Math.sin(dLon / 2) * Math.sin(dLon / 2);
    const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
    return R * c * 1.3; 
  },

  /**
   * إدارة دورة حياة العوامة الصامتة للراكب وإنهاء الطلب تلقائياً عند انعدام الكباتن
   */
  evaluateBuoyLifeCycle: (elapsedSeconds: number): { scanLevel: number; shouldPurge: boolean; notifyNoCaptains: boolean } => {
    if (elapsedSeconds >= 180) {
      return { scanLevel: 2, shouldPurge: true, notifyNoCaptains: true };
    }
    const scanLevel = elapsedSeconds <= 15 ? 1 : 2;
    return { scanLevel, shouldPurge: false, notifyNoCaptains: false };
  },
  
  calculateOSRMFare: (baseFare: number, distanceKm: number, durationMins: number, tariffPerKm: number, tariffPerMin: number): number => {
    return Math.round((baseFare + (distanceKm * tariffPerKm) + (durationMins * tariffPerMin)) * 100) / 100;
  },
  
  enforceMarketBrakes: (proposedFare: number, standardFareTest: number): { status: 'NORMAL' | 'AMBER_WARNING' | 'CRIMSON_BLOCK' } => {
    const deviationPercent = ((standardFareTest - proposedFare) / standardFareTest) * 100;
    if (deviationPercent >= 15.0) return { status: 'CRIMSON_BLOCK' };
    if (deviationPercent >= 10.0) return { status: 'AMBER_WARNING' };
    return { status: 'NORMAL' };
  },

  enforceRiderFirewall: (rider: any) => {
    if (rider.consecutiveCancellations >= 3) {
      rider.behaviorSafetyScore -= 0.5;
      if (rider.behaviorSafetyScore < 4.2) {
        rider.isPurged = true;
        throw new Error("KERNEL_PURGE: Account terminated due to malicious behavior.");
      }
    }
  }
});

Object.freeze(RadarAntiCheatKernel);
```

---

## البند الخامس: ميثاق التمهيد والموافقة القانونية على المعالجة الطرفية (Edge Processing Agreement)

* **تمهيد ورؤية تشاركية:** يعمل نظام "الرادار الذكي" وفق رؤية تقنية وإنسانية تهدف إلى كسر احتكار التكاليف التشغيلية العالية. نحن لا نفرض رسوماً خفية لأننا لا نعتمد على بنية تحتية مركزية مكلفة، بل نعتمد على "التشارك السيادي" لموارد المعالجة.

### 1. مبدأ المعالجة الطرفية (سيادة الجهاز)
* بقبولك استخدام تطبيق "الرادار الذكي"، فإنك توافق على أن يقوم التطبيق بإجراء عمليات الحساب الجغرافي والفرز الأولي للطلبات مباشرة على جهازك الخاص (`Edge Computing`). هذا الإجراء يضمن أن جهازك يعمل كـ "رادار محلي" مستقل، مما يلغي الحاجة إلى وسيط مركزي مكلف.

### 2. الغاية من التشارك التقني
* الهدف الأساسي من استخدام قدرة المعالجة في أجهزة المستخدمين هو تحقيق "صفرية التكلفة التشغيلية". هذا التوفير التقني لا يذهب لأرباح المنصة، بل يعود مباشرة وبشكل كامل إلى "محفظة المستخدم" (راكباً كان أو ناقلاً)، من خلال تقليص العمولات والرسوم التقليدية إلى أدنى مستوياتها، مما يجعل تكلفة التنقل هي الأعدل والأقل في الميدان.

### 3. خصوصية البيانات واللحظة الصفرية
* المعالجة التي تتم على جهازك تقتصر فقط على البيانات اللازمة لإتمام عملية "المصافحة الذرية" (الربط بين الطلب والعرض). لا يقوم النظام بسحب أي بيانات شخصية أو ملفات خارج نطاق المهمة اللوجستية. جهازك هو "المُحكم الأول" في تحديد المسافة والوقت وفق مرجعيات الخرائط العالمية المعتمدة لدينا.

### 4. كفاءة الاستهلاك
* تم تصميم "الرادار الذكي" ليكون "صديقاً للموارد"؛ حيث يتم استهلاك الحد الأدنى من طاقة البطارية وبيانات الإنترنت، بما يضمن استدامة عمل جهازك مع الحفاظ على دورك الفعّال في شبكة النقل الذكية.

### 5. الالتزام بالشفافية
* إن مساهمتك بجزء بسيط من قدرة معالجة جهازك هي التي تجعل التوفير حقيقة واقعة، وهي الضمانة الوحيدة لبقاء النظام حراً من رسوم الاستضافة المركزية التي تنعكس على كاهل المستخدمين في الأنظمة الأخرى.

---

**الختم المعرفي السيادي وشهادة الإيداع للنسخة V2.6-Secured:**
*تم توطيد كامل الفصول والمحركات وحفظها بنجاح تام ومطلق في خزنة عمليات الرادار.*
