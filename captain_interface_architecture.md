# 📜 الدستور التنفيذي والمعماري الواجهي لكابتن الرادار (V5.5)
## Executive Architectural & Technical Specification: Captain Interface

---

## 🏛️ تمهيد وهدف الوثيقة
تهدف هذه الوثيقة إلى تقديم دليل تنفيذي وهندسي كامل لبناء وتطوير **واجهة الناقل / الكابتن (Captain/Driver Interface)** في نظام "الرادار الذكي". تم تقسيم الواجهة إلى مراحل متسلسلة (Phases) تغطي دورة حياة الكابتن من مرحلة الهوية والتوثيق، مروراً بالمحفظة التشغيلية والرادار الجغرافي، وصولاً إلى كوابح الأسعار وجدار الحماية الأمنية.

---

## 📐 الهوية البصرية ونظام التصميم المعتمد (Design System)
تلتزم جميع شاشات ومكونات واجهة الكابتن بنظام الألوان والتدرجات التالية لتوفير تجربة فخمة ومريحة للعين في ظروف القيادة المختلفة:

```typescript
// tailwind.config.js - الإعدادات البصرية المعتمدة
module.exports = {
  theme: {
    extend: {
      colors: {
        radar: {
          bg: '#0B0F19',       // الكحلي الليلي الداكن جداً (الخلفية الجدارية)
          card: '#161F30',     // لون البطاقات الطافية المستقر (Elevated Slate)
          border: '#243249',   // الحدود الخافتة (1px)
          teal: '#14B8A6',     // الأخضر الفيروزي المشع (زر التفاعل والأرقام الحيوية)
          blue: '#3B82F6',     // الأزرق المتوسط المعياري
          textMain: '#F8FAFC', // الأبيض العاجي المريح للعناوين
          textSub: '#94A3B8',  // الرمادي المزرق للفقرات والبيانات الثانوية
          amber: '#F59E0B',    // لون التحذير (الانحراف السعري 10%)
          crimson: '#EF4444'   // لون الحظر والمنع (الانحراف 15% / Kill-Switch)
        }
      }
    }
  }
}
```

---

## 🧬 المرحلة الأولى: الهوية والتوثيق والترقيم الذري (Phase 1: Auth & Atomic Identity)

### 1.1 المفهوم والمحددات التشغيلية
* **بروتوكول التحقق والتسجيل:** يتطلب تسجيل السائق التحقق من رقم الهاتف واستكمال بيانات الهوية المهنية (المكتب / الشركة / مستقل) وتحديد لواء الموطن الأصلي (`homeDistrict`).
* **بروتوكول صفرية العمولة:** يُعفى الناقل إعفاءً كاملاً ومطلقاً من أي عمولة تقتطعها المنصة ($0.00$ عمولة).
* **التثبيت السيادي (PWA Standalone):** تُثبت الواجهة كتطبيق ويب تقدمي بحجم تخزيني لا يتجاوز 100KB لضمان خفة الأداء وتحرير البطارية.

### 1.2 خوارزمية الترقيم الذري (`serial_id`)
تعتمد عملية إصدار رقم الهوية الفريد للكابتن على معاملة ذرية آمنة (`runTransaction`) تضمن عدم تكرار الأرقام مطلقاً تحت ظروف السباق البياناتي (Race Conditions).

#### المسار البرمجي للعملية:
`src/components/views/auth-view.tsx` ➜ `handleRegistrationSubmit()` ➜ `runTransaction()`

```
[USER ACTION] إدخال بيانات الهوية والتأكيد
       │
       ▼
🛡️ [VALIDATION GATE] فحص Zod Schema (رقم الهاتف، نوع المركبة، المكتب/الشركة)
       │
       ▼
🔒 [ATOMIC TRANSACTION] - runTransaction(db)
       │
       ├─► 📖 [READ] قراءة counterRef = doc(db, 'system_counters', 'driver_serial')
       │      ├─ إذا كان موجوداً: currentCount = snapshot.data().current_count
       │      └─ إذا لم يكن موجوداً: currentCount = 1000
       │
       ├─► 🔢 [CALCULATE] newCount = currentCount + 1
       │      └─ serialId = `D-${newCount}` (مثال: "D-1001")
       │
       └─► 💾 [WRITE] معاملة كتابة مزدوجة لا تقبل التجزئة:
              1. transaction.set(userRef, { ..., serial_id: serialId, role: 'driver', trustRating: 5.0 })
              2. transaction.set(counterRef, { current_count: newCount })
```

---

## ⏱️ المرحلة الثانية: المحفظة التشغيلية وشحن الساعات (Phase 2: Wallet & Refill Engine)

### 2.1 فلسفة المحفظة الميدانية
* **نظام شراء الوقت (Hours Wallet):** لا يطالب الكابتن بنسبة من رحلاته، بل يشتري باقات ساعات نشطة للطيران في الميدان (مثال: 24 ساعة، 72 ساعة).
* **الشحن الجغرافي اللامركزي ($0.00$ بنكية):** لتجنب عمولات بوابات الدفع، يتم الشحن محلياً عبر:
  1. قنوات الدفع الرقمية المحلية الأردنية (CliQ، زين كاش، أورنج ماني، إي فواتيركم) المربوطة بـ `homeDistrict`.
  2. المندوب المالي المعتمد في اللواء الجغرافي.

### 2.2 الهيكل البرمجي لحالة المحفظة محلياً (IndexedDB / Local Logic)
```typescript
interface CaptainWalletState {
  captainId: string;
  serialId: string; // e.g., "D-1001"
  walletHoursRemaining: number; // مقدرة بالدقائق/الساعات
  isFlightActive: boolean; // حالة طيران السائق في الميدان
  homeDistrict: string; // لواء الموطن الثابت
  trustRating: number; // رصيد المناعة السلوكية (من 0.0 إلى 5.0)
}
```

---

## 📡 المرحلة الثالثة: الرادار الجغرافي والمصافحة الذرية (Phase 3: Spatial Radar & Handshake)

### 3.1 معمارية معالجة الحافة للخرائط (Edge Computing & MapLibre)
* **استئصال جوجل المدفوعة:** الاعتماد الكامل على `MapLibre GL JS` و `h3-js` لمعالجة الخرائط والمواقع مجاناً ($0.00$) على GPU الهاتف.
* **التشفير الجيومكاني:** يتم تحويل موقع السائق والراكب محلياً إلى خلية سداسية (`H3 Index` بدقة `Resolution 9`).
* **السيادة البصرية:** تُعرض الخريطة الملاحية الدقيقة للوصول الفعلي **مرة واحدة فقط** فور حدوث "المصافحة الذرية"، وتُعدم من الذاكرة فور انتهاء الرحلة.

### 3.2 مراقبة الخمول والتنظيف الآلي (Idle Monitor Engine)
لحماية صالة المزاد ومنع تلوث الميدان بسائقين غير متفرغين:
1. **تنبيه الدقيقة 9 (Min 9 Warning):** يظهر إشعار وميض برتقالي ينبه الكابتن بمرور 9 دقائق من الخمول دون تفاعل.
2. **الإخراج التلقائي بالدقيقة 10 (Min 10 Auto-Exit):** عند اكتمال الدقيقة 10 من الصمت، تُنهى باقة الطيران النشطة آلياً، ويُسحب الكابتن من صالة المزاد.

### 3.3 دورة حياة الرحلة والمصافحة (Trip Lifecycle & Dropping)
```
[صالة المزاد النسيجية] ──► [طلب الراكب (H3 Cluster)]
         │
         ▼
[تقديم عرض السائق] ──► (تطبيق كوابح Fare_test)
         │
         ▼
🤝 [المصافحة الذرية (Atomic Handshake)]
         ├─► تجميد سعر الرحلة فوراً (يُحظر تعديله)
         ├─► فتح خريطة الملاحة الحرة للوصول
         ├─► بدء المؤقت (ETA & Expiry Time)
         │
         ▼
🏁 [إتمام الرحلة أو سقوطها]
         └─► إعدام بيانات الملاحة من الذاكرة + تحديث السجلات مجمعة (1 Write Only)
```

---

## ⚖️ المرحلة الرابعة: ضبط التسعير وكوابح حرق الأسعار (Phase 4: Pricing Control & Anti-Dumping)

### 4.1 حزمة الاختبار المعيارية (`Fare_test`)
لمنع ظاهرة حرق الأسعار (Dumping) أو استغلال الميدان، يقوم النظام آلياً باختبار سعر الكابتن بناءً على رحلة افتراضية مرجعية: **5 كم + 10 دقائق**.

### 4.2 مستويات الانحراف والردع الصامت (Deviation Matrix)
$$Ratio = rac{| Fare_{Captain} - Fare_{Base} |}{Fare_{Base}}$$

| نسبة الانحراف | نوع الإشعار واللون | الأثر البرمجي والردع الصامت عند الحافة |
| :--- | :--- | :--- |
| **0% - 9.9%** | 🟢 طبيعي (Normal) | قبول العرض بشكل طبيعي واستمرار الحركة في الميدان. |
| **10% - 14.9%** | 🟧 تحذير وميض (`Amber Warning`) | 1. إظهار وسم "سعر محروق قد يؤثر على الجودة" للراكب.<br>2. خفض رتبة السائق آلياً وتزاح حزمته للاحتياط.<br>3. عرض إعلانات المنفعة للراكب. |
| **15% فما فوق** | 🔴 قفل ومنع (`Crimson Lockout`) | 1. **قفل حقل الإدخال تماماً** وتجميد زر "إرسال العرض".<br>2. **استحواذ الذكاء الاصطناعي على مسرح الشاشة** لعرض إعلانات مهنية موجهة للكابتن (قطع غيار/زيوت). |

---

## 🛡️ المرحلة الخامسة: جدار الحماية والأمن ومفتاح الصعق الجنائي (Phase 5: Anti-Cheat & Kill-Switch)

### 5.1 قفل الثغرات الثلاث (Kernel Anti-Cheat)

1. **ثغرة التلاعب بالوقت المحلي (Device Time Cheat):**
   * يتم قراءة "النبض الشبكي التفاضلي" (`Network Time Delta`) عند أول اتصال بالوثيقة العامة.
   * أي محاولة لتأخير أو تقديم ساعة الهاتف يدوياً تؤدي إلى تجميد قمرة العمليات فوراً.
2. **ثغرة الانقطاع الشبكي الأعمى (Ghost Disconnection):**
   * يتم حبس حالة العدادات في محفظة مشفرة (`IndexedDB`) بختم رقمي مجزأ (`Cryptographic Hash`).
   * يُحظر دخول صالة المزاد إلا بعد إجراء "مصافحة تصفوية صامتة" عند عودة التغطية.
3. **مقاومة هجمات الإغراق (DDOS & Fluctuation):**
   * تقييد الكابتن بعرض نشط واحد فقط في صالة المزاد.

### 5.2 بروتوكول مفتاح الصعق الجنائي (`The Ultimate Kill-Switch`)
عند استشعار خطر أمني، أو تلقي بلاغ جنائي، أو رصد مخالفة جسيمة من قِبل المشرف عبر الصندوق الأسود:

```
[SOVEREIGN KILL-SWITCH SIGNAL]
       │
       ▼
💥 إطلاق النبضة السيادية إلى هاتف الكابتن
       │
       ├─► 1. Service Worker ينهي باقة الساعات المتبقية فوراً ويصادرها.
       ├─► 2. إسقاط رصيد المناعة السلوكية كاملاً (trustRating = 0.0).
       ├─► 3. تحويل حالة الحساب محلياً وفورياً إلى: [ 🚫 محظور جنائياً ].
       └─► 4. Object.freeze يمنع أي تعديل على الذاكرة في الـ Runtime.
```

### 5.3 تجميد النواة البرمجية (Kernel Lockdown)
```typescript
// إنفاذ التجميد الدستوري الحازم لمنع التلاعب الجنائي بقيم الذاكرة في الـ Runtime
export const RadarCaptainAntiCheatKernel = Object.freeze({
  validateDeviceTime: (networkTime: number, localTime: number) => {
    const delta = Math.abs(networkTime - localTime);
    if (delta > 300000) { // انحراف أكبر من 5 دقائق
      throw new Error("🚨 خرق أمني: تم رصد تلاعب بالتوقيت المحلي للهاتف.");
    }
  },
  enforceFareGuard: (fareInput: number, baseFare: number) => {
    const deviation = Math.abs(fareInput - baseFare) / baseFare;
    if (deviation >= 0.15) {
      return { allowed: false, action: 'LOCKOUT_AND_SHOW_PROFESSIONAL_ADS' };
    }
    if (deviation >= 0.10) {
      return { allowed: true, action: 'AMBER_WARNING_AND_RANK_DEMOTION' };
    }
    return { allowed: true, action: 'NONE' };
  }
});

Object.freeze(RadarCaptainAntiCheatKernel);
```

---

## 🏛️ خلاصة التقديم التنفيذي
تم تصميم واجهة الكابتن لتعمل وفق أعلى معايير الأمان والهيبة التقنية (`Prestige`)، مع ضمان **صفرية التكلفة الشبكية ($0.00$)**، وحماية الميدان من التلاعب المالي أو السلوكي، وتحويل وقت الانتظار أو التجميد إلى فرص إعلانية مربحة ومفيدة.